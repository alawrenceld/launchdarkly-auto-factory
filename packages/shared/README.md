# shared

Code shared across packages: common types, the LaunchDarkly API client (with
configurable base URL, so it can target the production API or the LD-internal
non-prod instance), and config schemas for the files under `config/`.
